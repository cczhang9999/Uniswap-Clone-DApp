version: '3'
services:
  seata-server:
    image: seataio/seata-server:1.7.1
    hostname: seata-server
    container_name: seata-server
    ports:
      - "8091:8091"
      - "7091:7091"
    environment:
      - SEATA_IP=127.0.0.1
      - SEATA_PORT=8091
    restart: always
